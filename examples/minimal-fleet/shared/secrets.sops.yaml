# In a real fleet this file is SOPS-encrypted: `sops shared/secrets.sops.yaml`.
# It's left as plain YAML here so this example's `validate` command runs
# with zero AGE-key/vault setup — see the top-level README's "Auth"
# section for how secrets actually work in a real fleet.
admin:
  contact_email: oncall@example.com
